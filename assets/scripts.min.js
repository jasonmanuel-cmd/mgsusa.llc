(function() { 'use strict'; try { // Mobile menu toggle const toggle = document.querySelector('[data-menu-toggle]'); const mobileNav = document.querySelector('[data-mobile-nav]'); if (toggle && mobileNav) { toggle.addEventListener('click', function() { const open = toggle.getAttribute('aria-expanded') === 'true'; toggle.setAttribute('aria-expanded', String(!open)); toggle.setAttribute('aria-label', open ? 'Open navigation' : 'Close navigation'); mobileNav.classList.toggle('is-open', !open); document.body.classList.toggle('menu-open', !open); }
); mobileNav.querySelectorAll('a').forEach(function(link) { link.addEventListener('click', function() { toggle.setAttribute('aria-expanded', 'false'); toggle.setAttribute('aria-label', 'Open navigation'); mobileNav.classList.remove('is-open'); document.body.classList.remove('menu-open'); }
); }
); }
}
catch (e) { console.error('Menu toggle initialization failed:', e); }
try { // Year footer document.querySelectorAll('[data-year]').forEach(function(el) { el.textContent = new Date().getFullYear(); }
); }
catch (e) { console.error('Year footer update failed:', e); }
try { // Tracking-ready hooks. Replace these CustomEvents with GTM/GA4 and CRM events at launch. document.querySelectorAll('a[href^="tel:"]').forEach(function(link) { link.addEventListener('click', function() { if (window.CustomEvent) { window.dispatchEvent(new CustomEvent('mgs:call-click', { detail: { href: link.href, page: location.pathname }
}
)); }
}
); }
); document.querySelectorAll('[data-lead-form]').forEach(function(form) { form.addEventListener('submit', function() { if (window.CustomEvent) { window.dispatchEvent(new CustomEvent('mgs:lead-submit', { detail: { service: form.querySelector('[name="service"]')?.value || '', page: location.pathname }
}
)); }
}
); }
); }
catch (e) { console.error('Event tracking initialization failed:', e); }
try { // Scroll-reveal animation with IntersectionObserver const revealEls = document.querySelectorAll('.reveal'); if (revealEls.length > 0 && 'IntersectionObserver' in window) { const io = new IntersectionObserver(function(entries) { entries.forEach(function(e) { if (e.isIntersecting) { e.target.classList.add('revealed'); io.unobserve(e.target); }
}
); }
, { threshold: 0.12 }
); revealEls.forEach(function(el) { io.observe(el); }
); }
else if (revealEls.length > 0) { revealEls.forEach(function(el) { el.classList.add('revealed'); }
); }
}
catch (e) { console.error('Scroll reveal animation failed:', e); }
try { // Brand reveal animation - welcoming door opening with logo (only if element exists) var brandReveal = document.getElementById('brandReveal'); if (brandReveal) { // Auto-open doors after 2.5 seconds setTimeout(function() { brandReveal.classList.add('brand-reveal-opened'); // Hide the reveal screen after animation completes setTimeout(function() { brandReveal.style.opacity = '0'; setTimeout(function() { brandReveal.style.display = 'none'; document.body.style.overflow = ''; }
, 600); }
, 1800); }
, 2500); }
}
catch (e) { console.error('Brand reveal animation failed:', e); }
}
)();