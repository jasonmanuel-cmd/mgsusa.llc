# Domain Documentation Layout

This repository uses a **single-context** layout:

- `CONTEXT.md` at the repository root — high-level domain glossary, ubiquitous language, and cross-cutting concerns
- `docs/adr/` — Architecture Decision Records (one per significant decision, numbered sequentially)

No per-component or per-subdomain context files are maintained.